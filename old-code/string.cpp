#include <bits/stdc++.h>
using namespace std;

void solve(){
   string a,b;
   cin>>a>>b;
   if(a.size()==2||(a[a.size()-1]==0&&b[b.size()==0]||a==b)){
     cout<<"YES"<<endl;
     return;
   }
   else{
       
   }
   
}
int main() {
    int t;
    cin>>t;
    while(t--){
        solve();
    }

    return 0;
}